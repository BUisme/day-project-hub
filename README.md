# ทำไปทำไม⁇ – Day Project Hub

หน้าเว็บศูนย์รวมลิงก์โปรเจกต์ของเดย์ พร้อม:

- Splash / Loading Screen
  - ข้อความ: "เว็บสำหรับคนว่างๆ"
  - Sub: "รักนะไออ้วนไอหนาหมา🐶🤍"
  - วงกลมหมุน
  - แสดงประมาณ 3 วินาที แล้วค่อยหาย
- หน้า Project Hub หลังโหลด
  - หัวเว็บ: "ทำไปทำไม⁇"
  - Subtitle: "ผมสร้างเพื่อให้หายคิดถิงคนๆหนึ่ง🙃"
  - ไอคอนหัวเว็บ: 🐶
- Sidebar เมนู:
  - หน้าแรก
  - โปรเจกต์ทั้งหมด
  - โปรเจกต์แชทลับ
  - เกี่ยวกับผม
- ฟีเจอร์:
  - Search ค้นหาชื่อโปรเจกต์ / คำอธิบาย / tag
  - Filter ตามหมวด (แชทลับ / เครื่องมือ / อื่น ๆ)
  - Dark / Light mode toggle (จำโหมดล่าสุดด้วย localStorage)
  - Animation ตอน hover การ์ดและปุ่ม
  - แสดงจำนวนโปรเจกต์ที่กรองแล้ว / ทั้งหมด
  - Footer ข้อความปิดท้าย

## การเพิ่มโปรเจกต์

เปิด `index.html` แล้วค้นหาส่วน:

```js
const projects = [
  {
    id: "secret-message-qr",
    title: "Secret Message Link + Reply + QR",
    type: "Web / Message",
    category: "message",
    secret: true,
    description: "เว็บส่งข้อความลับ ใส่เพลง ช่องทางติดต่อ มีตอบกลับได้ + นับจำนวนครั้ง + QR code พร้อมส่งให้คนพิเศษ",
    url: "https://<your-username>.github.io/secret-message-link-reply-qr/",
    tags: ["secret", "message", "qr", "reply"],
    badge: "Pinned"
  },
  ...
];
```

เพิ่มโปรเจกต์ใหม่ใน array นี้ โดยใช้ฟิลด์:

- `id`: ชื่อสั้น ๆ ไม่ซ้ำ (ใช้ภาษาอังกฤษ / ตัวเลข)
- `title`: ชื่อโปรเจกต์ (แสดงบนการ์ด)
- `type`: ประเภท เช่น "Web / Message", "Tool", ฯลฯ
- `category`: `"message"`, `"tool"`, หรือ `"other"`
- `secret`: `true` ถ้าเป็นโปรเจกต์แชทลับ (ใช้กับเมนู "โปรเจกต์แชทลับ")
- `description`: คำอธิบายสั้น ๆ
- `url`: ลิงก์โปรเจกต์ (เช่น GitHub Pages)
- `tags`: array ของ tag เช่น `["secret", "esp32"]`
- `badge`: ข้อความพิเศษเล็ก ๆ มุมขวาล่างของการ์ด

ตัวอย่างโปรเจกต์ใหม่:

```js
{
  id: "step-up-converter",
  title: "Step-Up Converter Control UI",
  type: "Web / Control",
  category: "tool",
  secret: false,
  description: "หน้า UI สำหรับควบคุมและดูค่าของโปรเจกต์ Step-Up Converter ของเดย์",
  url: "https://<username>.github.io/step-up-converter-ui/",
  tags: ["power", "esp32", "ui"],
  badge: "Lab"
}
```

## Deploy บน GitHub Pages

1. สร้าง repo ใหม่บน GitHub เช่น `day-project-hub`
2. อัปโหลดไฟล์:
   - `index.html`
   - `README.md`
3. ไปที่ **Settings → Pages**
4. ในหัวข้อ **Source** เลือก:
   - `Deploy from a branch`
   - Branch: `main`
   - Folder: `/ (root)`
5. กด Save แล้วรอให้ deploy เสร็จ
6. จะได้ลิงก์ประมาณ:

   `https://<username>.github.io/day-project-hub/`

ส่งลิงก์นี้ให้คนอื่น เพื่อเข้ามาเลือกดูโปรเจกต์ทั้งหมดของเดย์ในหน้าเดียว
